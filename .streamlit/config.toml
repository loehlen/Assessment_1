[theme]
# Accent colour for Streamlit's own widgets (radio buttons, focus
# rings, links), matching the pink used in shared.py
primaryColor = "#9B6574"

# Very light pink instead of Streamlit's grey, used for the sidebar,
# the chat input box and other panels
secondaryBackgroundColor = "#FCF9FA"